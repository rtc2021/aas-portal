[build]
  base = "portal"
  command = "cd ../netlify/functions && npm install && cd ../../portal && npm run build"
  publish = "dist"
  functions = "../netlify/functions"
  ignore = "exit 1"

[functions]
  node_bundler = "esbuild"

[functions."copilot"]
  timeout = 30

[[redirects]]
  from = "/api/copilot"
  to = "/.netlify/functions/copilot"
  status = 200

[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"
